import * as categoryFilters from './category-filters';
import * as products from './products';
import * as cart from './cart';
import * as auth from './auth';

export const Api = {
  categoryFilters,
  products, 
  cart, 
  auth
}